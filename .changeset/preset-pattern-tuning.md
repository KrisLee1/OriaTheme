---
"@oriatheme/presets": patch
---

Retune preset patterns: Punchcard dots are larger and more widely spaced in both modes, Sketchbook moves to a dot `pattern.background` with a 1px grid `pattern.surface`, and Theorem gains a paper-grain `pattern.background` in both modes.
