import Link from "next/link";
import { ArrowRight, Code2, Palette, Puzzle } from "lucide-react";
import type { Locale } from "@/lib/i18n";

export function DocsHome({ locale }: { readonly locale: Locale }) {
  const zh = locale === "zh";
  return <><header className="docs-hero"><p>{zh ? "OriaTheme 文档" : "OriaTheme documentation"}</p><h1>{zh ? "从主题开始，建立一致的界面体验。" : "Start with a theme. Build a coherent interface."}</h1><span>{zh ? "选择使用主题或开发集成路径。每一页都从可观察的任务开始。" : "Choose the use-themes or developer-integration path. Every page starts with an observable task."}</span></header><div className="docs-path-grid"><Link href={`/${locale}/docs/use/editor`}><Palette /><h2>{zh ? "使用主题" : "Use themes"}</h2><p>{zh ? "浏览预设、创建我的主题，并在浏览器本地编辑、保存和导出。" : "Browse presets, create My themes, and edit, save, and export locally in the browser."}</p><ArrowRight /></Link><Link href={`/${locale}/docs/develop/quick-start`}><Code2 /><h2>{zh ? "开发集成" : "Developer integration"}</h2><p>{zh ? "安装 Runtime 与框架适配层，让现有组件消费主题变量。" : "Install the runtime and framework adapter, then let existing components consume theme variables."}</p><ArrowRight /></Link></div><section className="docs-home-section"><div><p>{zh ? "常用任务" : "Common tasks"}</p><h2>{zh ? "直接前往下一步" : "Go straight to the next task"}</h2></div><div className="docs-task-links"><Link href={`/${locale}/docs/develop/quick-start`}>{zh ? "开发快速开始" : "Developer quick start"}<ArrowRight size={16} /></Link><Link href={`/${locale}/docs/develop/packages`}>{zh ? "选择公开包" : "Choose packages"}<ArrowRight size={16} /></Link><Link href={`/${locale}/docs/api/core`}>{zh ? "查看 Core API" : "Browse Core API"}<ArrowRight size={16} /></Link><Link href={`/${locale}/docs/use/presets`}>{zh ? "选择官方预设" : "Choose a preset"}<ArrowRight size={16} /></Link><Link href={`/${locale}/docs/use/appearance`}>{zh ? "设置外观模式" : "Set appearance"}<ArrowRight size={16} /></Link><Link href={`/${locale}/docs/use/editable-styles`}>{zh ? "查看可编辑样式" : "See editable styles"}<ArrowRight size={16} /></Link></div></section><section className="docs-version-note"><Puzzle size={20} /><p>{zh ? "文档示例只使用已发布的公开 exports。在线编辑器默认在浏览器本地处理主题内容。" : "Examples use published public exports only. The online editor processes theme content locally in the browser by default."}</p></section></>;
}
