<script setup lang="ts">defineProps<{ modelValue: string }>(); const emit = defineEmits<{ "update:modelValue": [value: string] }>();</script>
<template><label data-oria-editor-search :data-has-value="Boolean(modelValue) || undefined"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="6.5" /><path d="m16 16 4 4" /></svg><span class="oria-editor-visually-hidden">Search all theme tokens</span><input type="search" placeholder="Search names, paths, or roles" :value="modelValue" @input="emit('update:modelValue',($event.target as HTMLInputElement).value)" @keydown.escape="modelValue && (emit('update:modelValue',''), $event.preventDefault())" /></label></template>
